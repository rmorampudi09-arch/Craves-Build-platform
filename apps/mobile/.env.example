# Public runtime configuration schema only. Never commit real environment files or secrets.
# Copy this file to an ignored environment file and inject deployment-specific values in CI/release tooling.
CRAVES_ENVIRONMENT=development
CRAVES_API_BASE_URL=https://api.example.invalid
