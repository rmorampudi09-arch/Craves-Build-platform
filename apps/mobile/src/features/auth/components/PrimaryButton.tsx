import React from 'react';
import {ActivityIndicator, Pressable, StyleSheet, Text, View, ViewStyle} from 'react-native';
import {colors, radius, spacing} from '../../../design/tokens';
import {Icon, IconName} from '../../../shared/components/Icon';
interface Props{label:string;onPress:()=>void;loading?:boolean;disabled?:boolean;variant?:'primary'|'outline';style?:ViewStyle;leftIcon?:IconName;rightIcon?:IconName}
export function PrimaryButton({label,onPress,loading=false,disabled=false,variant='primary',style,leftIcon,rightIcon}:Props){const off=disabled||loading;const fg=variant==='primary'?colors.white:colors.flameRed;return <Pressable accessibilityRole="button" accessibilityState={{disabled:off,busy:loading}} disabled={off} onPress={onPress} style={({pressed})=>[styles.base,variant==='primary'?styles.primary:styles.outline,off&&styles.disabled,pressed&&!off&&styles.pressed,style]}>{loading?<ActivityIndicator color={fg}/>:<View style={styles.row}>{leftIcon?<Icon name={leftIcon} size={20} color={fg}/>:null}<Text style={[styles.label,variant==='outline'&&styles.outlineLabel]}>{label}</Text>{rightIcon?<Icon name={rightIcon==='chevron'?'chevron-right':rightIcon} size={20} color={fg}/>:null}</View>}</Pressable>}
const styles=StyleSheet.create({base:{minHeight:56,borderRadius:radius.md,alignItems:'center',justifyContent:'center',paddingHorizontal:spacing.lg,marginTop:spacing.md},primary:{backgroundColor:colors.flameRed,shadowColor:colors.flameRed,shadowOpacity:.25,shadowRadius:10,shadowOffset:{width:0,height:5},elevation:3},outline:{backgroundColor:colors.white,borderWidth:1,borderColor:colors.flameRed},disabled:{opacity:.48},pressed:{transform:[{scale:.985}],opacity:.92},row:{flexDirection:'row',alignItems:'center',justifyContent:'center',gap:10,width:'100%'},label:{fontSize:16,fontWeight:'600',color:colors.white},outlineLabel:{color:colors.flameRed}});
