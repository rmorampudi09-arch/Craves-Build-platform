# Landing reference scope control

This change is intentionally limited to `apps/customer-web-next` public landing presentation and its handover/test coverage. It does not alter backend APIs, customer/chef authenticated application behavior, Firebase configuration, Cashfree configuration, Azure resources, APIM policy, DNS, data models, pricing, commissions, delivery rules, or compliance logic.
