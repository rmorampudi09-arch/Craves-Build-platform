# Public launch closure: evidence checkpoint

This is an implementation checkpoint, not public-launch acceptance. The source plan has 46 findings. No finding is counted Accepted without its full runtime, review and owner-evidence gate. The final real payment/delivery/bank journey is NOT RUN.

## Changes in this work

### Continuation after owner confirmation (supersedes older state below)

PR363 merged as `da34ac40ce644b84826cdf29c37d2cdd1a3baad6` with complete main regression35115644866 successful. Web attempts39096 and39100 both stopped BEFORE live image publication/deployment. The former caught Next-generated tracked-source drift; committing the generated declaration fixed it without weakening the clean-source guard. The latter caught desired/revision default-representation differences; read-only39102 identified exactly four differences without exporting settings. A narrow comparator now preserves the desired-state fingerprint while resolving only documented running defaults. PR364 includes that correction, tracking progress preservation and aggregate-only email health inspection. Deployment and controlled mailbox acceptance are still open at this entry.

Read-only39098 found one canonical verified identity, one projected identity, no incomplete canonical states, no profile/chef-application mismatches, no pending projections, and one provider-accepted challenge in the preceding24 hours with no recorded failed/unknown deliveries. These are aggregate observations, not inbox proof and not an assertion about a specific customer. See `EMAIL_OPERATIONAL_HEALTH.md`.

Main rule 83269016 now permits the owner's sole approval: second-person approval is off, while PRs, exact-source tests, up-to-date branches, resolved conversations, no-admin-bypass and force-push/deletion protections remain. PR360 merged as `111160c1a71674c8b0c178a55884e389c97ffb8c` from tested candidate `b9f9ee0de0d8d76126a7974c0db3607008851884`. Full regression35106628087 passed 1199 backend tests, 615 web tests and 51 connected synthetic checks. This is source evidence, not acceptance of all functions.

Azure39089 successfully reconciled the collection/resource policy representations. Run39091 then applied both scoped finance privacy policies and verified matching repaired structure; both stored hashes are `0228e53486bfffef88a53b3a29b95a53c7b76e20d80fa15f36a309816dd872ae`. The run remains FAILED because the immediate chef-route check still saw old headers. No write was retried. At 14:28:34 UTC independent anonymous probes on all four paths showed 401 with private/no-store/max-age=0 and Pragma:no-cache; synthetic-invalid-token checks all showed 401/no-store/no-cache. Delayed propagation is consistent with this sequence. Authenticated/error/static-cache acceptance remains open. PR360 comment5699164872 retains the operational receipt.

PR362 merged the guarded customer-web rollout asb0cc44f574eb4fff7a31682fd8baaa14f57b2700. Full merged-main regression35112960978 passed all four jobs. Azure pipeline93/run39096 was then started with production mode, explicit replacement confirmation, exact release SHA/image tag and that successful regression run. Deployment verification is still pending at this entry. F04 stays open until its deployment receipt and controlled mailbox acceptance exist.

Azure122/run39094 succeeded at15:02:05UTC with a strictly read-only check of35 applied versioned migrations across Auth, User/Chef and Notification. All filenames, checksums and success flags matched the reconciled source, including AuthV15/V16, User/ChefV12 and NotificationV7. See `APPLIED_EMAIL_HISTORY.md` and its machine receipt. No database/customer/flag writes or messages were made. Canonical profile projection and mailbox acceptance remain separate.

An intermittent web test failure on older merged-main111160c1 was reproduced as a fake-clock/test-effect ordering race. The resend test now waits for effect installation and verifies the59-second disabled boundary and60-second enabled boundary; no application countdown was changed. All16 email UI tests passed five consecutive local runs; the later exact-source complete regressions and separate admin/explorer checks passed. The earlier failed run remains failed evidence. The older chronology below is retained as history and must not override this continuation.

1. F01: added a read-only metadata collector and six boundary/redaction tests. Azure pipeline122/run39076 succeeded at 17:38 IST on source e5be02603e8ca57c301327b9f972e9d4b12f987c. It found 12 Container Apps, 39 APIs and 207 operations. No configuration drift was seen in the sanitized capture interval. Secret values and private customer data were not read or published.
2. F02: reproduced the subscription schema comparison failure against disposable PostgreSQL. PostgreSQL's `name` type truncated the concatenated sorting identity; equivalent constraint sets appeared in different orders. Corrected the audit's text type and canonicalized only the outer constraint collection. Added eight positive/negative comparator cases. No production migration was altered.
3. F03: added exact-source, missing-test and no-skip regression evidence checks. Main protection is saved and read back (rule 83269016), requiring one review, dismissal of stale approvals, resolved conversations, up-to-date code and the GitHub Actions exact-SHA aggregate, with no administrator bypass/force pushes/deletions. Test-only PR 361 proved an intentional source failure makes the required aggregate fail while backend/web skip; the owner UI showed merging blocked. The PR was closed without merging or deployment. Stale-green-head and deployment-wrapper evidence remain OPEN. The combined gate was restored selectively from PR348, retaining newer checkout, referral, email migration numbering and admin admission fixes.
4. F04/F05 candidate: restored the compatible web verification routes, panel, profile/registration/chef entry points and session-race protections from PR348. Preserved newer admin redirects, bounded request method/signal handling, admin admission limits and their tests. Added duplicate/escaped duplicate JSON-key rejection and bounded Retry-After propagation. New verification surfaces are white. This candidate is NOT YET DEPLOYED.

## Test receipts

- Run35093933007, SHA30ecde707db39e0a6b846d5ded18d19ef1b301d0: subscription 62 passed, zero skipped; 12 Python gate tests passed. Artifact SHA256 `6e24b567233d992bb3fa51f1d15dbd1f80b145b584c29373abf8b33e1e1dea65`.
- Run35094977579, SHAb0b2ff74619fde36d55ee7bc383f09f8d4a3c426: all seven complete Java services passed, 1,199 tests, zero failed/errors/skips. Connected synthetic finance scenarios passed 51 checks. These exercise mocked providers and disposable databases, NOT live money.
- That run's whole-platform result remains RED: web had 196 Vitest and 293 Node cases passing but lacked eight mandatory verification/race suites. Retained full machine-readable evidence in `evidence/regression-baseline-35094977579.json`. Do not relabel it green.
- Updated web working tree: typecheck, lint, 322 Vitest and 293 Node tests passed locally using Node24.19.0. Final exact-commit CI and deployment must follow.
- Run35096170879, SHA24356acc1e66e596434bb4ae7db4709b8b21eec4: the complete aggregate is GREEN. All1,199 Java tests,322 Vitest tests,293 Node tests and51 connected synthetic checks passed with zero skipped tests. Clean install, lint, typecheck and production build passed. Machine evidence: `evidence/regression-35096170879.json`. This certifies that source, not a live deployment.
- A subsequent governance correction makes the aggregate run even if its source job fails, reject every non-success prerequisite, and compare the exact source output before checkout. Four regression tests execute the actual inline workflow guard (including all failed/skipped/cancelled/missing prerequisites); all21 evidence/workflow tests passed locally. PR triggering is unfiltered so documentation-only PRs cannot lack the required check. Cloud evidence for this later workflow revision must be recorded separately.
- Earlier attempts35094795984/35094845670 did not pass. The initial restored workflow referenced two absent historical deployment wrappers; the next needed full Git ancestry for existing release provenance tests. Those setup issues were corrected without disabling the existing ancestry assertion.

## Actual serving composition preserved

Auth, User/Chef and Notification still serve customer-email source4ad490bea2a7ae0ca2f00628e63d3b0e202f407c; Order serves customer-checkout source1e365e81d24297a92bb6774f5610cce6158d3f67. Integration still serves referral source036b3211b2e81465699766f7d8104a4199c67723. The existing Referral app is internal-only with all observed business/worker/public gates false. Production customer web remains source870f5293884888aa28f0c069b91a86d06492c9a2. Registry digest resolution was captured; tag text alone is not independent proof of pulled-image provenance.

Auth email migrations are V15/V16 on this reconciled deployment, not the older PDF's V10/V11. Existing V12-V14 referral history and V9.1 admin admission remain intact. User/Chef V12 and Notification V7 remain their respective email additions. A separate read of applied database history/checksums is still outstanding; healthy startup is not substituted for it.

## Current safety observations and limits

- All 12 observed apps have one active healthy revision and one replica; configured min/max is one. This does not prove durable worker ownership or load capacity.
- Auth rate-limit and token-revocation flags are false. No activation was performed by this checkpoint.
- The inspected resource group returned zero metric alerts. Its one inspected action group had no email, SMS or webhook receivers. This is not a subscription-wide claim and does not certify other alert channels.
- PostgreSQL16 has seven-day retention; geo-redundant backup and high availability are disabled. Earliest observed restore point was2026-09-09T14:03:03Z. No restore rehearsal has run and no paid resource was provisioned.
- APIM policy fingerprints, exact runtime/private configuration parity, applied migration checksums, durable worker leases, queue oldest age and human alert acknowledgement remain OPEN.
- Azure run 39078 at SHA bddc9a4491a8e9ff35a864d7db2e9298d587288c succeeded and recorded 247 scopes: global, 39 API and 207 operation policies. Its seven scope/redaction tests passed. It exports only policy byte hashes, sizes, scope identifiers and safe format labels, not XML or named values; product/workspace policies and fragments remain separate reconciliation.
- Complete regression run 35100342845 passed on e535e0fa5ec8339eebabb51af0a91691f6558444; source, backend, web and aggregate all succeeded. Machine evidence is retained in `evidence/regression-35100342845.json`. Subsequent commits must run their own checks.
- Finance cache privacy remains OPEN: four anonymous finance routes returned 401 with only `Cache-Control: private`. No customer body was read. Read-only runs 39081 and 39083 stopped on fingerprint/shape differences; no policy write was attempted. The inspector can now describe unexpected shapes without authorizing their transformation. See `FINANCE_RESPONSE_PRIVACY.md`.
- F04 acceptance needs an explicitly controlled mailbox and actual successful owner verification. Test fixtures are not proof of inbox delivery.
- Referral clarification: qualifying sales use the referred chef food subtotal; two INR 150 dishes qualify at INR 300. The owner explicitly confirmed a monthly referral-reward cap of INR 1,500 per chef, not an infrastructure budget or a limit on ordinary food earnings. Calendar/rounding, authoritative qualification/hold events and refund treatment still need a complete policy receipt before activation. No referral credits were created.
- Support primary/backup and hours, remaining referral and subscription commercial rules, genuine chef finance declarations, statutory document decisions and isolated restore/load budget still need owner facts. No fake records, credits, policy approval or commercial defaults will be inserted.

## Files / recovery / execution

Tests and workflows are under `scripts/launch`, `scripts/release`, `.github/workflows/launch-p0-ci.yml`, `.github/workflows/launch-regression-ci.yml`. Web changes are under `apps/customer-web-next/src/app/api/auth/email-verification`, `src/components/auth`, `src/components/profile`, chef boundaries, `src/screens/Profile`, `src/services/auth/cravesAuth.ts` and corresponding `src/lib/*email*`/session tests. Test-only dependencies are pinned in package files.

Run Python evidence tests with `python -m unittest discover -s scripts/launch -p test_launch_evidence.py`; run the complete web `npm ci`, `npm run typecheck`, `npm run lint`, `npm test`, `npm run build` with Node24. Java conditional database tests run only in the workflow's explicitly named disposable PostgreSQL/PostGIS databases. Never point those tests at live databases.

No app image, public gateway policy, live customer record or payment was changed in this launch-plan checkpoint. The previous installed Android02 APK is unchanged. Source is on engineering branch `fix/device-acceptance-cart-20260916`; no old PR was merged wholesale. Reverting this candidate means restoring only its selected source changes before any deployment; applied live migrations remain immutable. Any later web deployment must retain Firebase, payment mode, document controls and current service origins, and record an exact compatible rollback image.
