# Public launch closure execution

Authority: owner request of16 September2026 to execute the69-page public-launch fixing plan. All46 findings stay in scope; no item is accepted merely because an image is deployed. The owner's final real payment/delivery/bank journey remains NOT RUN and separate.

## Current package: P0

1. F01: read-only running-service inventory and source reconciliation; preserve today's customer fixes and earlier referral/finance releases.
2. F02: reproduce the exact subscription migration parity failure in an explicitly disposable local database, then correct unordered comparison without dropping structure or weakening negative checks.
3. F03: inspect actual required checks/rules, add exact-source no-skip evidence, and apply reviewed protection only after its actual checks exist.
4. F38/F40/F41/F42: establish read-only support/alert/backup/security baseline before any new activation.

Remaining order: P1 identity/onboarding, P2 finance/selling, P3 order operations, P4 communications, P5 subscriptions, P6 referrals, P7 safety convergence, P8 native/Academy. User approved proceeding through these packages; missing business evidence still cannot be fabricated.

## Existing changes versus this plan

Prior runs39064/39071/39073 repaired mobile review routes, durable checkout and email backend. The Final02 APK added PDF saving and same-identity password setup. These are useful partial evidence for F04/F05/F20/F32/F45, not full closure. Statutory invoices, web verification, actual mailbox delivery, store signing and all-platform acceptance are not established by that work.

## Read-only inventory

`scripts/release/capture-launch-runtime.py --output <private-artifact>/launch-runtime.json` runs through existing pipeline122 action `capture-launch-runtime`, service connection `Craves-Dev-Service-Connection`. It enumerates Container Apps and all active revisions, image/tag/digest metadata, safe boolean gates, APIM operation paths, backup configuration and alert/receiver counts. It never reads secret values, sends customer/provider requests, deploys or provisions resources. The wrapper rejects non-read command families. Raw settings, recipient destinations and private policy bodies are not published.

Its sanitized fingerprint does NOT prove literal/secret equality. Source tags remain unverified hints until reconciled to source and image labels. Active revisions are NOT durable worker-lease evidence. Database history, exact policy bodies, oldest queue age, restore rehearsal and human alert acknowledgement remain separate required evidence. Missing reads are explicit, never treated as zero.

## Local validation

Python: `python -m unittest discover -s scripts/release/tests -p test_launch_runtime_capture.py`.
Java: Java21/Maven; subscription parity uses only localhost and database `subscription_schema_test`, requires `CRAVES_DISPOSABLE_TEST_DATABASE=true` plus actual CI or `CRAVES_LOCAL_DISPOSABLE_PARITY=YES_LOCAL_DISPOSABLE_SUBSCRIPTION_ONLY`. It recreates only its synthetic subscription schema/history inside that database. Never use an application database.

## Manual/business gates

No paid resource is authorized by this package. Before isolated cloud restore/load, name resources, cost ceiling, expiry and cleanup owner. Chef declarations/finance classification, referral reward basis and allocation rules, subscription commercial rules, actual support/alert owners, privacy/invoice requirements and signing/store accounts require genuine owner facts. Preserve current architecture until a specific conflict is resolved. No fabricated sales, refunds, stock, balances, reviews or approvals.

## Delivery and recovery

Current engineering branch `fix/device-acceptance-cart-20260916`, baseline39fca66a. Do not merge old PR348 wholesale: it conflicts and predates later releases. No production mutation in P0; local test-only comparison can be reverted independently, while applied migrations remain immutable. `closure-register.json` is the46-finding register with reviewer/date/evidence unset until real acceptance. Source ZIPs contain complete changed files, never private PDFs or credentials.
