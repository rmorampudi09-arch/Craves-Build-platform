<policies>
  <inbound>
    <base />
    <!-- CRAVES_ADMIN_EXPLORER_V1. Signature, current session and role checks remain in the owning Java service. -->
    <choose>
      <when condition="@(!context.Request.Headers.ContainsKey(&quot;Authorization&quot;) || !context.Request.Headers.GetValueOrDefault(&quot;Authorization&quot;, &quot;&quot;).StartsWith(&quot;Bearer &quot;))">
        <return-response><set-status code="401" reason="Unauthorized" /><set-header name="Content-Type" exists-action="override"><value>application/json</value></set-header><set-header name="Cache-Control" exists-action="override"><value>no-store</value></set-header><set-body>{"code":"AUTHENTICATION_REQUIRED"}</set-body></return-response>
      </when>
    </choose>
    <validate-content unspecified-content-type-action="ignore" max-size="8192" size-exceeded-action="prevent" />
    <!-- Durable admission: owning backend enforces 20 reads per rolling minute per dataset, across all callers. -->
    <set-backend-service base-url="__BACKEND_URL__" />
  </inbound>
  <backend><base /></backend>
  <outbound><base /><set-header name="Cache-Control" exists-action="override"><value>no-store</value></set-header><set-header name="X-Content-Type-Options" exists-action="override"><value>nosniff</value></set-header><set-header name="X-Robots-Tag" exists-action="override"><value>noindex, nofollow</value></set-header></outbound>
  <on-error><base /><set-header name="Cache-Control" exists-action="override"><value>no-store</value></set-header></on-error>
</policies>
