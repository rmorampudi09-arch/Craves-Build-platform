package com.craves.user.repository;

import com.craves.user.entity.PreferenceMode;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PreferenceModeRepository extends JpaRepository<PreferenceMode, String> {
}
